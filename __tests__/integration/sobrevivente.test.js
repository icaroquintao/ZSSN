const { Sobrevivente } = require('./app/models');

describe("Register", () => {
    it("should create a valid survivor", async() => {
        const x = 2;
        const y = 3;

        const sum = x + y;

        expect(sum).toBe(7);
    });
});